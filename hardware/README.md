# hardware/

KiCad output for ToneRelay P4 belongs here (`tonerelay-p4/`).

Requirements, pin map, and layout rules: [docs/hardware/README.md](../docs/hardware/README.md).

Do not start a schematic until that packet is read. Do not store Wi-Fi passwords in this tree.
